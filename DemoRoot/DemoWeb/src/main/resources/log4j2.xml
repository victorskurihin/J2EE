<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <!-- Logging Properties -->
    <Properties>
        <Property name="PREFIX">su.svn.demo.web</Property>
        <Property name="EXT_PATTERN">%d{yyyy-MM-dd}-%i</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} %p %m%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <RollingFile name="fileRoot"
                     fileName="log/${PREFIX}-root-debug.log"
                     filePattern="log/${PREFIX}-root-debug-${EXT_PATTERN}.log">
            <LevelRangeFilter minLevel="INFO" maxLevel="TRACE" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="19500KB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <RollingFile name="fileDemo"
                     fileName="log/${PREFIX}-demo-debug.log"
                     filePattern="log/${PREFIX}-demo-debug-${EXT_PATTERN}.log">
            <LevelRangeFilter minLevel="INFO" maxLevel="TRACE" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="19500KB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="trace" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="fileRoot"/>
        </Root>

        <Logger name="su.svn.demo.persistent" level="debug" additivity="false">
            <AppenderRef ref="fileDemo" level="debug"/>
        </Logger>
    </Loggers>
</Configuration>
